# Project Context

## Product intent

- The repository is an opinionated, production-minded Next.js starter implemented through a complete personal-todo reference application; it is not a toy project or temporary experiment.
- A derived application is expected to replace mostly the domain and UI while retaining or adapting cross-cutting foundations such as authentication, persistence, CMS integration, validation, testing, documentation, and local quality tooling.
- The starter deliberately presents one preferred stack and architecture rather than becoming a configurable multi-stack framework.
- Design and implementation optimize for high reusable quality without excessive build time: use the simplest robust modern approach and add complexity only when it has a clear safety, correctness, operability, maintainability, or avoided-rework payoff.
- Stack selection follows [TD-028](decisions/TECHNICAL.md#td-028) and [RULE-012](RULES.md#rule-012): current documented defaults for coding-agent authors, with Drizzle, Better Auth, and Zod 4 kept as named exceptions.

## Repository landscape

- One standalone Next.js repository uses the installed DWF candidate distribution `0.1.0-proposal.1` under `.dwf/.framework/`.
- The framework machinery is supplied and read-only during ordinary project work. Project Workspace state lives outside `.dwf/.framework/`.
- Source areas currently present include `app/`, `components/`, `db/`, `lib/`, `src/`, `e2e/`, `migrations/`, `scripts/`, and `docs/`.
- The repository now contains the runnable authenticated todo reference: `app/` composes public landing, auth, dashboard, API, and Studio routes; `src/modules/` contains auth, landing, lists, and tasks; and `components/` contains the Focus Rail UI.
- `app/page.tsx` reads the published Sanity landing singleton through the landing infrastructure boundary; `app/(app)/dashboard/page.tsx` composes the authenticated list/task application path.
- `lib/auth.ts` contains the Better Auth email/password and magic-link configuration backed by the shared Drizzle client and explicit mail boundary. Local/test uses the mailbox, Preview suppresses sends, and Production can select the Resend adapter after configuration validation.
- `db/schema/auth.ts`, `db/schema/lists.ts`, and `db/schema/tasks.ts` contain the Better Auth and todo tables. `db/schema/logging.ts` adds the environment-local logging-policy table; all four are active schema exports.
- `src/sanity/` and `src/modules/landing/infrastructure/` contain the Sanity client/configuration, validated read path, and invalidation boundary.
- `src/test/`, `scripts/playwright-local/`, and `e2e/` contain the local PostgreSQL 18 Testcontainers and dedicated Playwright lifecycle. The normal browser suite contains eight Chromium journeys, including fresh signup, pending-access refusal, email verification and subsequent password sign-in.
- `src/shared/logging/` contains the database-independent Pino core and environment-local Drizzle settings store/cache. `scripts/logging/` provides protected inspection and revision-checked edits. List/task entries, dashboard/landing reads, auth mail, Sanity invalidation and idle pool failures now use it; [application evidence](../docs/agentforge/evidence/2026-09-19-logger-adoption.md) records local proof and limits. The [runbook](../docs/runbooks/logging.md) owns usage, event names and diagnosis. `src/shared/diagnostics/` holds the destination-routing Strategy contracts, dispatcher and startup provider selection; root `instrumentation.ts` selects Sentry or Better Stack (on `@sentry/core` 11.0.0) once per Node server instance, boundaries own explicit error reports and `onRequestError` owns unreported unhandled failures. Export stays off unless a provider is configured and policy enables it; hosted provider proof is still outstanding. The [diagnostics runbook](../docs/runbooks/diagnostics.md) owns setup and diagnosis.

## Technology facts

The package manifest includes Next.js, React, Better Auth, Drizzle, node-postgres, Zod, Sanity, shadcn-related UI packages, Vitest, Playwright, pnpm scripts, Husky, and lint-staged. `db/db.ts` uses the shared node-postgres Drizzle adapter and registers its bounded pool for Vercel Fluid Compute lifecycle management. Sanity is represented by the source client/configuration and landing read path. The ignored `.env.local` currently contains local application, Neon, and Sanity configuration; its `NEON_BRANCH=main` value is an observed target fact, not an approved Development or Production identity.

## Documentation facts

- Existing `docs/` contains supporting handbook, architecture, domain, data, development, runbook, and protocol material.
- Durable project decisions and open state live under `.dwf/decisions/`.
- Generated Agent/Human PRD and SPEC projections live under `.dwf/output/`.
- `.dwf/concepts/` contains derived explanations only; it does not own requirements.
- The installed `.dwf/.framework/**` is supplied DWF machinery and is not rewritten by ordinary project work; `.dwf/README.md` is project-specific Workspace orientation.

## Operational facts

- Local application database is Docker PostgreSQL 18 through `pnpm local:postgres` / `pnpm dev:local` (`scripts/local-postgres/`). Integration and Playwright use disposable Testcontainers, not the Compose volume.
- Environment profile parsing and pre-mutation guards live in `scripts/environment/` (`pnpm environment:inspect`). Their pure rules are shared with the runtime gate in `src/shared/environment/`, which refuses an unsafe profile/target/origin/dataset/mail combination before the database, auth or CMS client exists and needs only the pooled runtime URL. A process without `APP_ENV` keeps the unprofiled local behavior unless it runs on Vercel. `app/api/health/[component]` exposes public liveness plus bounded, single-flight database and CMS probes (`src/shared/health/`), protected by `HEALTH_PROBE_SECRET` in remote environments; the [operations runbook](../docs/runbooks/operations.md) owns them. Preview and Production delivery forward `APP_RELEASE_SHA`, the observed `DATABASE_ENDPOINT_HOST` and `HEALTH_PROBE_SECRET`, and their smoke checks the running release and readiness through those endpoints; the next hosted run of either needs `HEALTH_PROBE_SECRET` in its GitHub Environment, which is not yet provisioned. A failed protected Production release step now sends one release-failure Email through `src/shared/operational-alerts/` (NotificationPort + Resend adapter) from `scripts/deploy/production/notify.ts`; it needs a `RELEASE_ALERT_EMAIL` recipient secret in the `production` Environment, also not yet provisioned. See the [runtime validation section](../docs/runbooks/environment-profiles.md#runtime-validation).
- `.github/workflows/ci.yml` runs Quality and Harness on main pushes/pull requests without deployment or Production secrets. Preview and Production workflows are manual. T-22's owner-authorized hosted Preview completed on 2026-09-14 with real browser verification and identity-checked cleanup. T-23's protected exact-ref workflow subsequently released reviewed commit `d639dfeeeca2932606c652cf5305ca3e0cd87a89` on 2026-09-16. [Live release evidence](../docs/agentforge/evidence/2026-09-16-production-release-live.md) records approval, direct forward migrations, exact deployment, canonical smoke and recovery references. Each future hosted operation retains its authorization boundary.
- T-20 provisioned durable `development`, branch `br-super-leaf-axfwoi2e` in Neon project `curly-dust-60603928`, with no expiry. Guarded identity inspection, direct migration and ordinary seed are recorded in the testing ledger. The earlier expiring T-01 branch is historical. Do not infer Development or Production identity from `.env.local`.
- Production Resend uses the verified owner subdomain `auth.dim-stamatakis.dev` and the scoped GitHub `production` Environment. Required owner review, main-only deployment policy and disabled administrator bypass were observed. The initial mail-only check validated shape, not provider access. The first deployed send later exposed a credential/configuration mismatch; updating the protected key from the existing verified credential and redeploying the same SHA resolved it. The real magic-link message arrived in Gmail Inbox, established the private dashboard session and passed sign-out gating. The original adapter-only message had arrived in Spam and was manually moved; both results retain their separate limits. No receiving mailbox was configured.
- The [environment map](../docs/architecture/environments.md), [Preview runbook](../docs/runbooks/preview-delivery.md) and [Production release/recovery procedure](../docs/runbooks/production-release.md) describe the implemented commands. T-24's [complete pipeline matrix](../docs/agentforge/evidence/2026-09-16-pipeline-closeout.md) reconciles local refusal/failure tests and actual hosted proof; the environment, pipeline, Preview and release baseline contracts are verified.
- The committed migration chain under `migrations/` is Better Auth, lists/tasks, the T-26.2 logging-settings forward migration and the T-27.1 `auth_rate_limit` forward migration (auth-owned admission counters; not yet wired into Better Auth or applied to any hosted database). Read-only Production inspection after the earlier approved releases found six expected tables and two migration journal entries; that historical result does not establish the new settings migration in Production. Applied shared/Production history is immutable; use reviewed forward migrations. No Production seed or reset ran.
- `pnpm sanity:smoke` is the read-only published landing check. Actual signed Sanity webhook deliveries returned HTTP 200 on both Production deployments after identical-content publishes. The freshness/recovery contract combines that real delivery with existing local signature, relevance, duplicate and manual-recovery tests. Public Sanity project identity is configuration; webhook/recovery values remain secrets.
- [`TD-026`](decisions/TECHNICAL.md#td-026) remains the accepted environment matrix. Missing hosted prerequisites are not inferred from local files.
